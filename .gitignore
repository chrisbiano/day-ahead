# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode
.idea
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local tooling (machine-specific paths)
.claude

.vapid.local.txt

# Capacitor / Xcode build output. The iOS project itself IS tracked (it holds
# real config: bundle id, capabilities, entitlements), but nothing generated.
ios/App/build/
ios/App/App/public/
ios/App/DerivedData/
**/xcuserdata/
*.xcworkspace/xcshareddata/
# Supabase CLI local state (link target, versions) — not project config.
supabase/.temp/
